# Kind cluster for the k8s example. One control-plane node is enough: the actor
# cluster's members are pods, not Kubernetes nodes.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
